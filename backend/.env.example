# =============================================================================
# BACKEND ENVIRONMENT VARIABLES
# =============================================================================
# Copy this file to .env and fill in your actual values
# DO NOT commit .env file to version control

# -----------------------------------------------------------------------------
# DATABASE CONFIGURATION (REQUIRED)
# -----------------------------------------------------------------------------
# PostgreSQL connection string from Neon or any PostgreSQL provider
# Format: postgresql://user:password@host:port/database?sslmode=require
DATABASE_URL=postgresql://user:password@host:port/database?sslmode=require

# -----------------------------------------------------------------------------
# AUTHENTICATION & SECURITY (REQUIRED)
# -----------------------------------------------------------------------------
# Secret key for admin authentication (legacy + JWT fallback)
# Generate a secure random string: openssl rand -hex 32
ADMIN_SECRET_KEY=your-super-secret-admin-password-change-me

# JWT secret key for token signing (if not set, falls back to ADMIN_SECRET_KEY)
# Generate a different secure random string: openssl rand -hex 32
JWT_SECRET_KEY=your-jwt-secret-key-change-me

# JWT algorithm (default: HS256)
JWT_ALGORITHM=HS256

# JWT token expiration in minutes (default: 60)
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=60

# -----------------------------------------------------------------------------
# EMAIL SERVICE (REQUIRED for email notifications)
# -----------------------------------------------------------------------------
# Resend API key for sending emails
# Get yours at: https://resend.com/api-keys
RESEND_API_KEY=re_your_resend_api_key_here

# Email sender address (must be verified in Resend)
EMAIL_FROM=Your Name <contact@yourdomain.com>

# -----------------------------------------------------------------------------
# CONTACT INFORMATION (OPTIONAL)
# -----------------------------------------------------------------------------
# Your contact phone number (displayed in emails/forms)
CONTACT_PHONE_NUMBER=+1234567890
PHONE_NUMBER=1234567890

# Calendly scheduling link (for meeting bookings)
CALENDLY_LINK=https://calendly.com/your-username/30min

# -----------------------------------------------------------------------------
# API CONFIGURATION (OPTIONAL - has defaults)
# -----------------------------------------------------------------------------
# Frontend URL for CORS (default: https://arpitkumar.dev)
FRONTEND_URL=http://localhost:5173

# Backend API URL (used by frontend)
VITE_API_URL=http://localhost:8000

# CORS Origins - REQUIRED for Admin Panel + Main Portfolio!
# List domains that can access the backend API (comma-separated)
# 
# ⚠️ CRITICAL: Must include BOTH main portfolio AND admin panel domains
#    Without admin panel domain in CORS_ORIGINS:
#    - Admin panel login will fail with CORS error
#    - Leads dashboard won't load  
#    - All API calls from admin panel will be blocked
#
# Development setup (local):
# CORS_ORIGINS=http://localhost:5173,http://localhost:5174
#
# Production examples:
# CORS_ORIGINS=https://yourdomain.com,https://admin.yourdomain.com
# CORS_ORIGINS=https://arpitkumar.com,https://admin.arpitkumar.com
# CORS_ORIGINS=https://portfolio.vercel.app,https://admin-panel.vercel.app
# CORS_ORIGINS=https://portfolio.netlify.app,https://admin-panel.netlify.app
#
# Note: Use comma-separated values for multiple domains, no spaces
CORS_ORIGINS=http://localhost:5173,http://localhost:5174

# -----------------------------------------------------------------------------
# RATE LIMITING (OPTIONAL - has defaults)
# -----------------------------------------------------------------------------
# Public endpoint rate limit (default: 10/minute)
RATE_LIMIT_PUBLIC=10/minute

# Admin endpoint rate limit (default: 100/minute)
RATE_LIMIT_ADMIN=100/minute

# -----------------------------------------------------------------------------
# LEGACY/DEPRECATED (kept for backward compatibility)
# -----------------------------------------------------------------------------
# Google Apps Script URL (if using Google Sheets integration)
# VITE_GOOGLE_SCRIPT_URL=https://script.google.com/macros/s/...
