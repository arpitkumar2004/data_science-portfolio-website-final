# Lead Intelligence Stack - Architecture Diagram

## 📐 System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                    FRONTEND (React + SWR)                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────┐        ┌─────────────────────┐         │
│  │ Admin Dashboard     │        │  Public Forms       │         │
│  ├─────────────────────┤        ├─────────────────────┤         │
│  │ • Login (JWT)       │        │ • Contact Form      │         │
│  │ • Leads Table       │        │ • CV Request        │         │
│  │ • KPI Cards         │        │ • Collaboration     │         │
│  │ • Filters/Search    │        └─────────────────────┘         │
│  │ • Bulk Ops          │                                        │
│  └─────────────────────┘                                        │
│          ↓                                                      │
│     SWR Hooks                                                   │
│  ┌─────────────────────┐                                        │
│  │ useLeads()          │  Auto-refresh every 30s                │
│  │ useLeadStats()      │  Revalidate on focus/reconnect         │
│  │ useOptimistic...()  │  Optimistic updates                    │
│  └─────────────────────┘                                        │
│          ↓                                                      │
│     adminAPI Service                                            │
│  (Centralized API Client)                                       │
│          ↓                                                      │
│  Authorization: Bearer <JWT_TOKEN>                              │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                           ↓ HTTPS ↓
                           
┌─────────────────────────────────────────────────────────────────┐
│                  BACKEND (FastAPI)                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ Rate Limiting Middleware (SlowAPI)                       │   │
│  │ • Public: 10 requests/minute                             │   │
│  │ • Admin: 100 requests/minute                             │   │
│  └──────────────────────────────────────────────────────────┘   │
│                          ↓                                      │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ PUBLIC ENDPOINTS (No Auth)                               │   │
│  ├──────────────────────────────────────────────────────────┤   │
│  │ POST /api/submit-contact                                 │   │
│  │ POST /api/v1/request-cv                                  │   │
│  │ • Capture: IP, user-agent, referrer, origin              │   │
│  │ • Store in metadata JSONB                                │   │
│  │ • Rate limited                                           │   │
│  └──────────────────────────────────────────────────────────┘   │
│                          ↓                                      │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ ADMIN ENDPOINTS (JWT Auth + RBAC)                        │   │
│  ├──────────────────────────────────────────────────────────┤   │
│  │ GET    /admin/leads               [List all]             │   │
│  │ GET    /admin/leads/{id}          [Get one]              │   │
│  │ GET    /admin/leads/stats         [KPIs]                 │   │
│  │ PATCH  /admin/leads/{id}/status   [Update status]        │   │
│  │ PATCH  /admin/leads/{id}/priority [Update priority]      │   │
│  │ PATCH  /admin/leads/{id}/quality-score [Update score]    │   │
│  │ PATCH  /admin/leads/{id}/notes    [Update notes]         │   │
│  │ PATCH  /admin/leads/{id}/tags     [Update tags]          │   │
│  │ DELETE /admin/leads/{id}          [Delete]               │   │
│  │ DELETE /admin/leads/bulk-delete   [Bulk delete]          │   │
│  │ PATCH  /admin/leads/bulk-status   [Bulk status update]   │   │
│  │ GET    /admin/leads/search?q=...  [Search]               │   │
│  │ GET    /admin/leads/export?format=[csv|json]             │   │
│  │ Require: Authorization: Bearer <JWT_TOKEN>               │   │
│  │ Rate limited (100/min)                                   │   │
│  └──────────────────────────────────────────────────────────┘   │
│                          ↓                                      │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ AUTH ENDPOINTS (JWT Management)                          │   │
│  ├──────────────────────────────────────────────────────────┤   │
│  │ POST /admin/login         → JWT token                    │   │
│  │ POST /admin/logout        → Revoke token                 │   │
│  │ GET  /admin/me            → Current user info            │   │
│  │ GET  /admin/validate      → Validate token               │   │
│  │ POST /admin/login/legacy  → Old token system (backward)  │   │
│  └──────────────────────────────────────────────────────────┘   │
│                          ↓                                      │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ SERVICES (Business Logic)                                │   │
│  ├──────────────────────────────────────────────────────────┤   │
│  │ lead_service          → Create/read/update/delete leads  │   │
│  │                         + analytics (velocity, conv)     │   │
│  │ auth_service_v2       → JWT token management + RBAC      │   │
│  │ email_service         → Send acknowledgments/CVs         │   │
│  │ serializers           → Convert models to JSON           │   │
│  └──────────────────────────────────────────────────────────┘   │
│                          ↓                                      │
│                   SQLAlchemy ORM                                │
│                          ↓                                      │
└─────────────────────────────────────────────────────────────────┘
                           ↓ SQL ↓
                           
┌─────────────────────────────────────────────────────────────────┐
│                   DATABASE (PostgreSQL)                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Table: contact_leads                                           │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ CORE FIELDS                                              │   │
│  ├──────────────────────────────────────────────────────────┤   │
│  │ • id (PK)                                                │   │
│  │ • name, email, subject, message, company                 │   │
│  │                                                          │   │
│  │ NEW FIELDS (Lead Intelligence Stack)                     │   │
│  ├──────────────────────────────────────────────────────────┤   │
│  │ • lead_type: ENUM                                        │   │
│  │   └─ contact | cv_request | collaboration                │   │
│  │                                                          │   │
│  │ • status: ENUM (NEW LIFECYCLE)                           │   │
│  │   └─ unread → processing → contacted → archived          │   │
│  │                                                          │   │
│  │ • priority: ENUM                                         │   │
│  │   └─ low | medium | high | urgent                        │   │
│  │                                                          │   │
│  │ • metadata: JSONB                                        │   │
│  │   └─ {ip_address, user_agent, referer, origin}           │   │
│  │                                                          │   │
│  │ • created_at: TIMESTAMP                                  │   │
│  │   └─ For lead velocity analytics                         │   │
│  │                                                          │   │
│  │ • updated_at: TIMESTAMP (AUTO)                           │   │
│  │   └─ Auto-updated by trigger                             │   │
│  │                                                          │   │
│  │ LEGACY FIELDS (Preserved)                                │   │
│  ├──────────────────────────────────────────────────────────┤   │
│  │ • timestamp, flagged, role, form_type                    │   │
│  │ • quality_score, internal_notes, tags                    │   │
│  │ • contact_history, follow_up_date, last_contacted        │   │
│  │ • source                                                 │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
│  INDEXES (For Performance)                                      │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ • idx_contact_leads_created_at    → Velocity queries     │   │
│  │ • idx_contact_leads_status        → Status filtering     │   │
│  │ • idx_contact_leads_email         → Deduplication        │   │
│  │ • idx_contact_leads_priority      → High-priority alerts │   │
│  │ • idx_contact_leads_role          → Conversion metrics   │   │
│  │ • idx_contact_leads_updated_at    → Change tracking      │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
│  TRIGGER                                                        │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ trigger_contact_leads_updated_at                         │   │
│  │ └─ Auto-updates updated_at on every row modification     │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔐 Security Flow

```
┌─────────────────┐
│  Admin User     │
└────────┬────────┘
         │
         │ 1. POST /admin/login + password
         ↓
┌─────────────────────────────────────────┐
│  Backend Auth Service                   │
│  ├─ Validate password                   │
│  ├─ Generate JWT token                  │
│  │  └─ Payload: {sub: "admin", role: "admin", exp: ...}
│  └─ Return: {access_token, token_type: "bearer", expires_in: 3600}
└────────┬────────────────────────────────┘
         │
         │ 2. Store token in sessionStorage
         │
         │ 3. All subsequent requests:
         │    Authorization: Bearer <JWT_TOKEN>
         ↓
┌─────────────────────────────────────────┐
│  Backend Rate Limiter                   │
│  ├─ Check IP address                    │
│  ├─ Check request count (last 60s)      │
│  └─ Return 429 if exceeded              │
└────────┬────────────────────────────────┘
         │
         │ 4. JWT Verification
         │    ├─ Extract token from header
         │    ├─ Verify signature (using JWT_SECRET_KEY)
         │    ├─ Check expiration
         │    └─ Return 401 if invalid/expired
         ↓
┌─────────────────────────────────────────┐
│  RBAC (Role-Based Access Control)       │
│  ├─ Check token.role == "admin"         │
│  ├─ Return 403 if insufficient perms    │
│  └─ Grant access if authorized          │
└────────┬────────────────────────────────┘
         │
         │ 5. Execute endpoint logic
         │    └─ Query database
         │    └─ Capture metadata
         │    └─ Update lead
         ↓
    ✅ Return result
```

---

## 📊 Data Flow - Admin Panel

```
USER ACTION                 SWR BEHAVIOR              DATABASE
═════════════════════════════════════════════════════════════════

(1) Admin opens dashboard
         │
         ├─ SWR fetches /admin/leads (with JWT)
         │                    │
         │                    ├─ Network request
         │                    ├─ Results cached locally
         │                    └─ Re-fetched every 30s
         ├─ SWR fetches /admin/leads/stats
         │                    └─ (same)
         │
         ↓
    UI shows leads & KPIs


(2) Admin edits lead status
         │
         ├─ UI INSTANTLY updates (optimistic)
         │  └─ Changes displayed immediately
         │
         ├─ SWR calls PATCH /admin/leads/1/status
         │        │
         │        ├─ Server updates database
         │        │           │
         │        │           ├─ Trigger fires
         │        │           ├─ updated_at = NOW()
         │        │           └─ Returns updated lead
         │        │
         │        └─ SWR revalidates (/admin/leads)
         │           └─ Refreshes full list
         │
         ├─ If error:
         │  └─ UI reverts to previous state
         │
         ↓
    ✅ Lead updated + UI sync with server


(3) Browser tab loses focus → gains focus
         │
         ├─ SWR detects focus change
         │
         ├─ SWR revalidates:
         │  ├─ GET /admin/leads
         │  └─ GET /admin/leads/stats
         │
         ├─ Checks for changes since last fetch
         │
         ├─ Updates UI if new leads arrived
         │
         ↓
    ✅ Admin sees latest data


(4) Timer: 30 seconds passes
         │
         ├─ SWR automatically revalidates
         │  (even if user hasn't clicked anything)
         │
         ├─ New leads from other sources appear
         │
         ├─ Stats updated with new velocity/conversion
         │
         ↓
    ✅ Real-time dashboard without manual refresh


(5) Admin submits public form (from another page)
         │
         ├─ Form submitted to POST /api/submit-contact
         │        │
         │        ├─ Rate limit check (10/min)
         │        │
         │        ├─ Create lead with:
         │        │  ├─ status = "unread"
         │        │  ├─ lead_type = "contact"
         │        │  ├─ metadata = {ip, user-agent, referer}
         │        │  ├─ created_at = NOW()
         │        │  ├─ priority = "medium" (default)
         │        │
         │        └─ Return success
         │
         ├─ (Meanwhile, Admin Dashboard SWR revalidates every 30s)
         │
         ├─ Within 30 seconds:
         │  ├─ SWR detects new lead
         │  ├─ Dashboard UI refreshes
         │  ├─ "Unread" KPI increments
         │  ├─ "Lead Velocity (24h)" increments
         │  └─ New lead appears in table
         │
         ↓
    ✅ Real-time visibility of incoming leads
```

---

## 🚀 Deployment Architecture

```
┌──────────────────────────────────────────────────────────────┐
│                    EDGE (CDN)                                 │
│                   Vercel Edge                                 │
│         (JavaScript, static assets cached)                    │
└──────────────────────────────────────────────────────────────┘
                        ↓ HTTPS ↓
┌──────────────────────────────────────────────────────────────┐
│              FRONTEND (Vercel)                                │
│         Next.js / React + SWR                                 │
│  ┌────────────────────────────────────────────────────────┐  │
│  │ Admin Dashboard                                         │  │
│  │ Public Forms                                            │  │
│  │ Portfolio Pages                                         │  │
│  └────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────┘
                        ↓ HTTPS ↓
                        API Calls
┌──────────────────────────────────────────────────────────────┐
│              BACKEND (Render)                                 │
│           FastAPI + SlowAPI                                   │
│  ┌────────────────────────────────────────────────────────┐  │
│  │ Rate Limiting (SlowAPI middleware)                     │  │
│  │ JWT Auth (jose, passlib)                               │  │
│  │ Business Logic (lead management)                       │  │
│  │ Email Service (Resend API)                             │  │
│  └────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────┘
                        ↓ TCP/IP ↓
                   (SSL/TLS over VPC)
┌──────────────────────────────────────────────────────────────┐
│              DATABASE (Neon PostgreSQL)                       │
│   ┌──────────────────────────────────────────────────────┐   │
│   │ contact_leads (with new schema)                       │   │
│   │ ├─ ENUM types (status, lead_type, priority)          │   │
│   │ ├─ JSONB metadata                                     │   │
│   │ ├─ Audit timestamps (created_at, updated_at)         │   │
│   │ ├─ Performance indexes                                │   │
│   │ └─ Auto-update trigger                                │   │
│   └──────────────────────────────────────────────────────┘   │
│   ┌──────────────────────────────────────────────────────┐   │
│   │ Backups:                                              │   │
│   │ ├─ Neon automatic backups (hourly)                    │   │
│   │ ├─ Point-in-time recovery (30 days)                  │   │
│   │ └─ Database encryption at rest                        │   │
│   └──────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────┘
```

---

## ✅ Feature Matrix

| Feature | Public | Admin | Real-time | Secure |
|---------|--------|-------|-----------|--------|
| Submit Leads | ✅ | ✅ | ✅ | Rate-limited |
| View Leads | ❌ | ✅ | ✅ (SWR) | JWT + RBAC |
| Edit Leads | ❌ | ✅ | ✅ (SWR) | JWT + RBAC |
| Delete Leads | ❌ | ✅ | ✅ (SWR) | JWT + RBAC |
| Bulk Operations | ❌ | ✅ | ✅ (SWR) | JWT + RBAC |
| KPI Dashboard | ❌ | ✅ | ✅ (Auto 30s) | JWT + RBAC |
| Metadata Capture | ✅ | ✅ | ✅ | Rate-limited |
| Audit Trail | N/A | ✅ | ✅ | JWT + RBAC |
| Export Data | ❌ | ✅ | ✅ | JWT + RBAC |

---

**Total System is:**
- ✅ Scalable (single table, no joins)
- ✅ Secure (JWT, RBAC, rate limiting)
- ✅ Real-time (SWR auto-refresh)
- ✅ Maintainable (modular code)
- ✅ Auditable (full timestamp tracking)
- ✅ Performant (indexes, caching)
