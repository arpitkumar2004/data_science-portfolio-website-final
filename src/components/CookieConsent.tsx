import React from 'react';

/**
 * Analytics now loads immediately from the GTM container in index.html.
 * This component stays in place so the existing lazy import remains valid.
 */
const CookieConsent: React.FC = () => {
  return null;
};

export default CookieConsent;
